const API_ORIGIN = process.env.REACT_APP_API_ORIGIN ?? '';
export const API = {
  items:     `${API_ORIGIN}/api/items`,
  auth:      `${API_ORIGIN}/api/auth`,
  discounts: `${API_ORIGIN}/api/discounts`,
};
export const apiUrl = (p) => `${API_ORIGIN}${p}`;
